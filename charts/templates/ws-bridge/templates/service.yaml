apiVersion: v1
kind: Service
metadata:
  name: {{ include "ws-bridge.fullname" . }}
  labels:
    {{- include "ws-bridge.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ws-bridge.selectorLabels" . | nindent 4 }}
