apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "playit.fullname" . }}
  labels:
    {{- include "playit.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
